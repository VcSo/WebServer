#ifndef SERVER_H
#define SERVER_H

#include <string>

#include "../Log/Log.h"

namespace vc
{
    class Server
    {
    public:
        Server();
        ~Server();
        Server(int port, )

    private:

    };
}

#endif SSERVER_H